PostalIQ Orders Register update

Runtime file:
  data/orders.json

The updated HTML reads data/orders.json as the master Orders Register.
The year-specific JSON files are included as source-year archives/reference datasets.

Coverage in master register:
  2020-2026
  137 order records currently loaded from the supplied 2020-2025 PDF compilations and 2026 JSON/source set.

Important:
  Supersession fields are intentionally not classified yet. They remain empty until each relationship is verified from explicit source wording. Do not infer supersession merely from similar subjects.
